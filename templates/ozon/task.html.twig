<div class="accordion" id="parentProductsAccordion{{ taskId }}" data-task-id="{{ taskId }}">
    {% for parentProductId, parentData in parentProducts %}
        <div class="accordion-item">
            <h2 class="accordion-header" id="headingParent{{ parentProductId }}">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseParent{{ parentProductId }}" aria-expanded="false" aria-controls="collapseParent{{ parentProductId }}">
                    {{ parentData.parentProduct.getKey() }} ({{ parentData.products|length }} varyasyon)
                </button>
            </h2>
            <div id="collapseParent{{ parentProductId }}" class="accordion-collapse collapse" aria-labelledby="headingParent{{ parentProductId }}" data-bs-parent="#parentProductsAccordion{{ taskId }}" data-type="child">
                <div class="accordion-body">
                    <div class="loading-spinner text-center" id="loadingChild{{ parentProductId }}" style="display: none;">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                    </div>
                    <div id="childProducts{{ parentProductId }}"></div>
                </div>
            </div>
        </div>
    {% else %}
        <div class="accordion-item">
            <div class="accordion-header">
                <p class="text-muted">No parent products available.</p>
            </div>
        </div>
    {% endfor %}
</div>
